export type MockInterviewRecord = {
  id: string;
  questionId: string;
  startedAt: string;
  duration: number;
};

export type RecentViewedQuestionRecord = {
  id: string;
  questionId: string;
  viewedAt: string;
};
